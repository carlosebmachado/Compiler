:- Test program
program {
    define {
        not variable
            natural INT_CONST = 10.
            real FLOAT_CONST = 5.0.
        variable
            char char_var.
            boolean bool_var.
    } 
    execute {
        set bool_var to true.
    }
}
